/**
 * Internal feature toggle:
 * - Set to `false` to completely disable AI evaluation (no API calls).
 * - Set to `true` only when you have OpenAI tokens/credits available.
 */
export const ENABLE_AI_EVALUATION = false

